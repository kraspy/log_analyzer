"""Infrastructure layer — external world implementations.

Provides concrete implementations of domain interfaces:
- Database (SQLAlchemy + asyncpg)
- Log parsers (combined, custom formats)
- AI providers (OpenAI, DeepSeek via pydantic_ai)
"""
