[project]
name = "log-analyzer"
version = "0.1.0"
description = "Nginx log analyzer — web app + CLI"
readme = "README.md"
requires-python = "==3.14.3"
dependencies = []

[tool.uv.workspace]
members = ["backend"]

[dependency-groups]
dev = [
    "pre-commit>=4.0",
]
